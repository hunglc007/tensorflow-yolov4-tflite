module.exports = {
    // Print trailing commas wherever possible when multi-line and where valid in ES5 (objects, arrays, etc.)
    trailingComma: 'all',
    // Use single quotes instead of double quotes, prettier defaults to double quotes
    singleQuote: true,
    // Print semicolons at the ends of statements.
    semi: true,
    //Use tabs instead of spaces
    useTabs: true,
    //Number of spaces per tab
    tabWidth: 4,
    //Space between brackets
    bracketSpacing: true,
    //Place the closing > on a new line
    jsxBracketSameLine: false,
    //Line length before wrapping
    printWidth: 120,
    //Avoid parenthesis around a sole arrow function parem
    arrowParens: "avoid",
};